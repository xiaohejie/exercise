#include <iostream>
using namespace std;
#include "deleteNum.h"
/*
	给一个数组nums和一个值val，你需要原地移除所有数值等于val的元素，并返回移除后数组的新长度；
	不要使用额外的数组空间，必须仅使用O(1)额外空间并原地修改输入数组
*/
void test() {
	//deleteNum(&nums , x);
}
int main1() {
	system("pause");
	return 0;
}